- [2026-05-25 16:49 CEST]: Local File Diet iOS MVP Implementation
  - *Details*: Built a complete iPhone-only SwiftUI project from the local specification, including app scaffold, logo/app icons, privacy manifest, local-only file import, compression review flow, progress/cancellation UI, result sharing/export, settings, local history, and Share Extension handoff.
  - *Tech Notes*: Added native Apple-framework services using UniformTypeIdentifiers, ImageIO, PDFKit, AVFoundation, PhotosUI, Photos, OSLog, and Swift concurrency. Implemented modular compression engines for images, PDFs, videos, ZIP packaging, and unsupported passthrough. Added an Xcode project generator script using the local `xcodeproj` Ruby gem, no runtime third-party app dependencies. Bundle ID: `com.simohue.localfilediet`; Share Extension bundle ID: `com.simohue.localfilediet.shareextension`; App Group placeholder: `group.com.simohue.localfilediet`.

- [2026-05-25 16:49 CEST]: Verification
  - *Details*: Built and tested the app on iOS Simulator using XcodeBuildMCP.
  - *Tech Notes*: `build_sim` succeeded with zero warnings/errors. `test_sim` succeeded with 11 passing tests covering target parsing, byte formatting, output filename generation, file type detection, temporary-file copy safety, cancellation behavior, image compression output, PDF vector/scanned heuristic, and video bitrate estimation.

- [2026-05-25 17:15 CEST]: Simulator Launch
  - *Details*: Restored the missing shared `LocalFileDiet` Xcode scheme and launched the app on the iPhone 17 simulator for manual testing.
  - *Tech Notes*: `build_run_sim` succeeded for bundle ID `com.simohue.localfilediet`.

- [2026-05-25 17:21 CEST]: Minimal Home Redesign
  - *Details*: Updated the home screen so the transparent logo is the primary visual element instead of the app name, with concise product copy and only the essential import actions visible.
  - *Tech Notes*: Removed the white background from `Logo.imageset/logo.png`, simplified `HomeView`, hid the navigation title, preserved the settings shortcut, and added Italian localizations for the new visible strings. Verified with `build_run_sim`, simulator screenshot, and `build_sim`.

- [2026-05-25 17:25 CEST]: English-Only App Text
  - *Details*: Converted the app back to English-only text by removing Italian translations from the string catalog.
  - *Tech Notes*: `Localizable.xcstrings` now keeps English source keys without `it` localizations, so SwiftUI text resolves to English even on an Italian simulator. Verified with `rg` for Italian strings and `build_run_sim` plus simulator screenshot.

- [2026-05-26 09:22 CEST]: Premium Compression Progress UI
  - *Details*: Replaced the simple linear compression loader with a richer attention-grabbing progress panel: animated circular progress, shimmering progress bar, phase timeline, privacy note, and stronger phase-specific iconography.
  - *Tech Notes*: Added `ProgressPresentation` smoothing so progress never visually moves backward when compression engines retry different strategies. Updated `FileReviewView` to apply monotonic progress updates. Added a unit test for non-regressing progress. Verified with `build_sim`, `test_sim -only-testing:LocalFileDietTests` (12 passed), and `build_run_sim`.

- [2026-05-26 09:24 CEST]: Result Home Return Action
  - *Details*: Added a result-screen action that returns the user to the Home screen after a completed compression so they can start a new import immediately.
  - *Tech Notes*: Added `onStartNewImport` to `ResultView` and wired `AppRootView` to clear the `NavigationStack` path. Verified with `build_sim`.

- [2026-05-26 09:28 CEST]: Paid Upfront Unlimited Access
  - *Details*: Converted monetization to a paid-upfront app model. After purchase/download, users have unlimited local compressions with no runtime trial, paywall, subscriptions, or in-app purchases.
  - *Tech Notes*: Removed runtime purchase gating, compression counters, paywall UI, StoreKit product loading, and restore-purchase actions. Settings now shows unlimited access included. App price must be configured in App Store Connect at EUR 2.99. Verified with `build_sim` and `test_sim -only-testing:LocalFileDietTests` (12 passed).

- [2026-05-26 09:49 CEST]: GitHub Pages Website and App Store Legal Links
  - *Details*: Added a modern static website for Local File Diet under `docs/`, including a product home page, Privacy Policy, Support page, Terms of Use, 404 page, sitemap, robots file, and optimized logo asset. The site is designed for deployment from the public GitHub repository via GitHub Pages and includes the Privacy Policy URL and Support URL needed for App Store Connect.
  - *Tech Notes*: Added dependency-free HTML/CSS/JS assets in `docs/` with canonical GitHub Pages URLs for `https://simo-hue.github.io/Local-File-Diet/`. Updated `SettingsView` with in-app links to the Privacy Policy, Support, and Terms pages so legal/support information is accessible from the app. No new runtime dependencies were added.

- [2026-05-26 09:50 CEST]: Review Advanced Toggle Layout Fix
  - *Details*: Fixed the Advanced disclosure section on the review screen so long toggle labels no longer push switches beyond the right edge after selecting an image.
  - *Tech Notes*: Constrained the review scroll content to the available width and replaced the Advanced toggles with a reusable responsive row that allows label wrapping while keeping switches inside the viewport. Updated the existing home UI test to match the current logo-first home screen and marked it `@MainActor` to remove Swift Concurrency warnings. Verified with `build_sim` and the focused `LocalFileDietUITests/LocalFileDietUITests/testHomeScreenLoads` UI test.

- [2026-05-26 10:15 CEST]: Contained Advanced Switch Rendering
  - *Details*: Hardened the review Advanced section layout so every switch is drawn inside a fixed safe trailing column and remains fully visible on the device width.
  - *Tech Notes*: Replaced the row-level native switch rendering in the Advanced section with a scoped `ToggleStyle` and custom switch shape. The toggles keep SwiftUI toggle bindings and accessibility identifiers, while the visible control uses explicit dimensions to avoid clipping inside `DisclosureGroup`. Verified with `build_sim` and simulator screenshot inspection on the Review screen.

- [2026-05-26 10:26 CEST]: Share Extension App Store Upload Fix
  - *Details*: Fixed the Share Extension `Info.plist` structure that caused App Store Connect error 90360 by nesting `NSExtensionActivationRule` under the required `NSExtensionAttributes` dictionary.
  - *Tech Notes*: No new dependencies were added. Verified the source plist with `plutil -lint`, verified the generated embedded `.appex` plist contains `NSExtensionAttributes`, and confirmed both Release simulator build and Release `iphoneos` build succeed.

- [2026-05-26 10:28 CEST]: App Store Connect Copy Sheet
  - *Details*: Added a copy-ready Markdown file with App Store Connect metadata for Local File Diet, including app description, subtitle, promotional text, keywords, review notes, privacy answers, URLs, permissions copy, screenshot captions, and optional Italian localization text.
  - *Tech Notes*: Created `APP_STORE_CONNECT_COPY.md`. No code, project settings, dependencies, or runtime behavior were changed.

- [2026-07-31 14:20 CEST]: Output Integrity Guard and Real ZIP Compression (Loop 1/5)
  - *Details*: Fixed two engine-level defects. (1) No engine checked whether its output was actually smaller than the input, so a compression pass that grew the file handed the user the larger file and reported "0%" as if that were a success. (2) `SimpleZIPWriter` wrote every entry with compression method 0 (STORE), so the ZIP feature never compressed anything — it only added header overhead and made files bigger.
  - *Tech Notes*: Added `OutputGuard.resolve(candidateURL:input:store:fileManager:)` in `CompressionHelpers.swift`; all four engines now funnel through it at a single call site each before building their `CompressionResult`, and fall back to the untouched original (re-extensioned to match the real bytes) with a new `CompressionWarning.keptOriginal`. Rewrote `ArchiveCompressionEngine` with a real DEFLATE writer using Apple's Compression framework (`compression_encode_buffer` + `COMPRESSION_ZLIB`, which emits the raw DEFLATE stream ZIP method 8 requires), per-entry STORE fallback for incompressible data, UTF-8 filename flag, real DOS timestamps, and explicit ZIP64 refusal. Added `SimpleZIPReader` (central-directory parse, stored + deflate, CRC verification, typed `ZIPError` for unsupported features) so importing a ZIP now re-packs its contents instead of wrapping the archive in another archive; unreadable archives fall back to wrapping with an explanatory warning. Entry names are normalised against path traversal. New dependency: `import Compression` (system framework). New tests: `LocalFileDietTests/ArchiveCompressionEngineTests.swift` (12 cases) plus 6 `OutputGuard` cases in `CoreUtilityTests.swift`.
  - *Verification*: Real repo sources compiled on macOS under `-swift-version 6 -strict-concurrency=complete` with zero warnings, then executed. `unzip -t` reports "No errors detected"; `zipinfo` shows `defN` for compressible entries and `stor` for random ones; `unzip` extracts our archives byte-identically (`cmp`); our reader round-trips archives produced by system `zip -0` and `zip -9`. Re-packing a stored archive went 670,452 -> 81,774 bytes (87.8% reduction) — the case that previously produced a *larger* file.
  - *Tooling*: Added `Scripts/add_file_to_project.rb` for surgical file registration. `Scripts/generate_project.rb` had drifted from the committed project (it wiped `DEVELOPMENT_TEAM`, downgraded `objectVersion` 54 -> 46, and dropped three Xcode-added build settings); pinned the team ID and object version, and documented that the surgical script is the one to use day to day.

- [2026-07-31 14:40 CEST]: Image Engine Correctness (Loop 2/5)
  - *Details*: Fixed five defects in `ImageCompressionEngine`. (1) `CompressionSettings.preserveTransparency` was never read by any code path, so the review screen's transparency toggle did nothing and alpha channels were discarded silently. (2) Image -> PDF was offered in the output picker but routed through `CGImageDestination` with a PDF UTI and a meaningless lossy-quality key, so it could not honour a size target. (3) Preserving metadata merged the *source* property dictionary wholesale into the destination, so a downsampled file advertised the original's pixel dimensions and a stale orientation. (4) With no target size the binary search exited on its first iteration, so "no size limit" produced almost no compression. (5) The estimate always claimed the target would be met, and the search re-decoded the full-resolution original once per quality trial.
  - *Tech Notes*: Added an `OutputPlan` shared by `estimate` and `compress` that decides output format and flattening once. Alpha is detected via `kCGImagePropertyHasAlpha` with a `CGImage.alphaInfo` fallback; with `.automatic` output the engine now picks HEIC (or PNG when HEIC encoding is unavailable) to keep alpha, and otherwise composites over opaque white through a `noneSkipLast` `CGContext` with a new `CompressionWarning.transparencyFlattened`. Image -> PDF became a dedicated `encodePDFPage` path (page size = pixels at 72 dpi, capped to a 2000 pt long edge, image embedded as JPEG so the quality search moves the final PDF byte count). Metadata preservation now copies only the EXIF/TIFF/GPS/IPTC sub-dictionaries and rewrites the stale dimension and orientation keys. Added an explicit `encodeAtNominalSettings` branch for the no-target case. The estimate now runs a real 512 px probe and extrapolates bytes-per-pixel, clamped to `[0.02x, 1.0x]` of the original. `searchForTarget` resamples once per candidate dimension and re-encodes that `CGImage` per quality trial.
  - *Verification*: A macOS harness mechanically ported from the real source file (imports + the UIKit PDF call only) was compiled under Swift 6 and executed. Alpha + explicit JPEG now yields RGBA `[254,254,254,255]` plus the flattening warning, versus `[0,0,0,255]` through an un-prefilled opaque context; alpha + automatic yields a HEIC that still decodes to alpha `0`. No-target `.smallestFile` went 6,616,494 -> 236,131 bytes (96.4%). A metadata-preserving run on a 4000x3000 source correctly declares 2048x1536 in both the file properties and EXIF. A target-driven run used 5 resamples for 44 encodes, where the old code used 44 resamples. An unreachable 1 KB target now estimates 132,329 bytes with `targetMayBeUnrealistic` instead of claiming ~983 bytes. New tests: 7 cases in `ImageCompressionEngineTests.swift`.
  - *Not verified here*: `UIGraphicsPDFRenderer` itself cannot run on macOS, so the image -> PDF page writer was proven by geometry-equivalent `CGContext` port only.

- [2026-07-31 15:15 CEST]: Hybrid PDF Engine (Loop 3/5)
  - *Details*: Rewrote `PDFCompressionEngine`. Previously a text/vector PDF was "compressed" by `document.dataRepresentation()`, which recompresses nothing, so the most common kind of PDF got zero reduction; `.smallestFile` rasterized every document unconditionally, destroying selectable text, links and form fields even on pure-text files; the raster path ran 4 DPI x 3 quality = 12 full-document sweeps with no binary search and no early exit; each page round-tripped through two full-size bitmaps with no pixel cap; and the "is this a scan?" test required more than 450 KB per page, so a moderately compressed scan was misread as text and left untouched.
  - *Tech Notes*: New `LocalFileDiet/Core/Compression/PDFPageWriter.swift` wraps `CGDataConsumer` + `CGContext(consumer:mediaBox:)`; `ImageCompressionEngine.encodePDFPage` was migrated onto it, so `import UIKit` and the obsolete `import MobileCoreServices` are gone from that file. Core Graphics was chosen over `UIGraphicsPDFRenderer` specifically because it also exists on macOS, which makes the engine executable and provable on the dev machine. The analyzer now inspects real content: it walks `/Resources` -> `/XObject` via `CGPDFDocument`, sums image bytes and pixels per page (sampling at most 8 pages plus a middle sample on long documents), and classifies each page as image-dominant or textual. Compression is a single hybrid pass: image-dominant pages are rasterized at a computed DPI and embedded as JPEG, textual pages are replayed with `CGContextDrawPDFPage` so their text stays selectable. Documents with no image-dominant pages return early with an honest `pdfTextOnly` warning instead of being rasterized. The 12-attempt sweep became one probe render plus at most 3 proportional correction passes. Every page raster is clamped to 4 megapixels and wrapped in an `autoreleasepool`. Title/author/subject/keywords are copied when metadata is kept. `QualityMode.pdfDPISequence` was replaced by `pdfDPIRange`; the unused `vectorPDFProtected` warning was deleted.
  - *Verification*: The real engine source compiles and runs unmodified on macOS (the port script substitutes only 3 instrumentation counters); 76 harness checks pass. Mixed 6-page document (4 text + 2 photos): 3,711,937 -> 1,118,868 bytes (30.1%), and all four text pages still return their exact original 2,082 characters from `PDFDocument.page(at:).string` in the OUTPUT file. Same fixture on `.smallestFile` at a 1/6 target: 382,329 bytes (10.3%), text still intact. 12-page scan: 22.1 MB -> 4.7 MB at a 1/4 target. Text-only PDF on `.smallestFile`: 0 render passes, 0 rasters, text preserved. Render passes never exceeded 1 probe + 1 pass in any fixture (bound is 4). Peak page raster 3,999,796 px, never above the 4 MP cap; an A0 poster page that would have been 34.8 megapixels uncapped rendered at 1682x2378. Wall clock on the 12-page scan: 0.62 s versus 6.83 s for a reimplementation of the old 12-attempt sweep. Cancellation throws `CancellationError` both immediately and mid-flight; progress fractions are monotonic in every run. New tests: `LocalFileDietTests/PDFCompressionEngineTests.swift` (8 cases), `PDFAnalyzerTests.swift` rewritten (5 cases).
  - *Known limitation*: a scanned PDF that has been OCRed has extractable text on every page, so no page is classified image-dominant and the file is returned unchanged. The warning now states this plainly rather than claiming the PDF contains no photos. Making this user-selectable is tracked in TO_SIMO_DO.md.

- [2026-07-31 16:05 CEST]: Precise Video Bitrate Targeting (Loop 4/5)
  - *Details*: Rewrote `VideoCompressionEngine`. It previously only asked `AVAssetExportSession` for named presets, which carry fixed internal bitrates, so it had no way to aim at a size: "Under 5 MB" was luck. It compensated by exporting the whole video up to three times and keeping the smallest result, making the slowest operation in the app up to 3x longer than necessary. `presetsFor(...).prefix(3)` also silently dropped the last fallback, and progress ran backwards between attempts because the outer loop reported `index / 3` while each export reported its own 0...1.
  - *Tech Notes*: New `LocalFileDiet/Core/Compression/VideoEncodingPlan.swift` holds all planning as pure functions with no AVFoundation types in any signature (`VideoSourceFacts` in, `VideoEncodingPlan` out), so it is unit-testable without media; `VideoBitratePlanner`'s two original static methods are kept and delegate to it. The plan subtracts an audio budget (128/96/64 kbps by quality mode, 0 when the source has no audio track), reserves container overhead, derives the video bitrate from target and duration with a 150 kbps floor, and chooses an output resolution by walking a ladder of standard long edges and keeping the largest whose bits-per-pixel-per-frame clears a codec-dependent threshold (H.264 ~0.07-0.10, HEVC about half). Dimensions are even-floored and never upscale the source. `settings.preferHEICWhenAvailable` now also selects HEVC for video. The engine transcodes with `AVAssetReader` + `AVAssetWriter` using `AVVideoAverageBitRateKey` plus `kVTCompressionPropertyKey_DataRateLimits`, preserves the track's `preferredTransform`, transcodes audio to AAC at the planned bitrate, reports one monotonic progress fraction from sample presentation timestamps, and does at most 2 encode passes (a second only when the first overshoots by more than 8%). `AVAssetExportSession` remains as a single-preset fallback with a new `videoPresetFallback` warning. `estimate` now reports the planned resolution, codec and predicted bytes instead of `target * 1.05`.
  - *Verification*: The real engine was compiled and executed on macOS against synthetic 1080p/4K/portrait fixtures with real AAC audio. Target versus actual on the 1080p fixture, all in ONE pass: 1 MB -> 965,739 (0.966, 480x270); 2 MB -> 1,923,016 (0.962, 854x480); 5 MB -> 4,773,627 (0.955, 1280x720); 10 MB -> 9,668,145 (0.967, 1920x1080). 4K at a 5 MB target -> 4,721,244 (0.944, downscaled to 1280x720). Every target met, none wastefully undershot. Orientation: a portrait source with a 90 degree transform produced an output with an identical transform and matching rendered aspect (0.5625), so rotation stayed in the transform rather than being baked into pixels. Audio output measured 93 kbps against 96 planned, format `kAudioFormatMPEG4AAC`. Progress across 304 samples never decreased. Cancellation throws `CancellationError` and leaves no partial file. 60 harness checks pass. Two design corrections came out of measurement: a 2% container reserve alone landed 0.6-3.6% OVER target, fixed with a measured headroom factor plus explicit data-rate limits; and even-rounding could round 999 px up past the source, fixed with even-floored ceilings. New tests: `VideoEncodingPlanTests.swift` (13 cases) plus 4 engine cases in `CoreUtilityTests.swift`.

- [2026-07-31 16:40 CEST]: UX Pass and Batch Compression (Loop 5/5)
  - *Details*: Fixed the user-facing controls that did nothing, then built the feature the app's own file picker already implied. Dead controls fixed: "Try Smaller" and "Better Quality" on the result screen called the same closure with re-read defaults, so two differently-labelled buttons did the identical nothing; history rows used `.swipeActions` inside a `VStack`, which only works inside a `List`, so swipe-to-delete had never worked and rows were not tappable at all; the review screen's preset picker was hardcoded to `.forms` while the real target came from saved defaults, so a user whose default was "Under 2 MB" saw "Under 10 MB"; there was no way to choose no size limit even though every engine handles a nil target; the estimate re-ran on every keystroke; `.onOpenURL` appended to the existing navigation stack, burying a file shared in from the extension; and the share extension relied on `extensionContext.open`, which is not dependable from a share extension, with no UI to report failure.
  - *Tech Notes*: New plain-value types carry the logic out of the view bodies so it can be tested and executed: `Core/Models/TargetSelection.swift` (`TargetSelectionState` as the single source of truth for preset / custom / no-limit, initialised from saved defaults, with validation), `Core/Models/SettingsAdjustments.swift` (`trySmaller()` / `betterQuality()` and their availability flags), `Core/Models/ResultPresentation.swift` (`ResultStatus`, so `.keptOriginal` no longer renders as a failure and a no-target run reports its actual reduction instead of "Target not reached"). `Features/Shared/CompressionSettingsSections.swift` factors the target/quality/output/advanced sections out of `FileReviewView` so the batch screen reuses them rather than duplicating them. Estimates are debounced ~300 ms with a token guard so a superseded estimate cannot overwrite a newer one. The share extension gained a status view with a real error path, uses a responder-chain walk to `UIApplication.open` as a fallback, and now accepts all attachments rather than breaking after one.
    Batch compression: importing 1 file keeps the existing flow untouched; 2+ files route to `.batch`. `Features/Batch/BatchRunner.swift` is an `@MainActor @Observable` runner over an injectable `BatchFileCompressing`, compressing sequentially (the PDF and video engines hold large buffers, so concurrency would spike memory), tracking per-item state, continuing past individual failures, and cancelling promptly. `BatchResultView` shows per-file before/after plus total bytes saved and offers Share all, Save all to Files, and Save all as ZIP — the last built with `SimpleZIPWriter.write(entries:to:)` off the main actor, which is only useful because loop 1 made that writer actually deflate.
  - *Verification*: SwiftUI views cannot be executed here, so the logic was deliberately kept outside them and a macOS harness compiled the REAL sources (verbatim for every model file) and ran 85 checks, 0 failures. The picker desync regression: saved default `veryStrict2` now shows "Under 2 MB" and hands the engines 2,000,000 bytes, and all 7 presets round-trip. No limit yields a nil target. Invalid custom input (`abc`, `-3`, `0`, `1,2,3`, `1 KB`) is rejected with an inline message and leaves the previous valid target intact; clearing the field falls back to the default rather than a stale value. Try Smaller (5 MB / balanced) and Better Quality (15 MB / bestQuality) now differ from each other and from the input, and each hides when it would be a no-op. Result headline for a kept original reads "Already as small as it gets" with neutral tone instead of "Target not reached" with a warning triangle. Batch: all-succeed collects 3/3 with progress 1.0 and 3 history entries; one failure leaves the other two succeeding and is not written to history; cancellation stopped in under a second against a 3 s stub with only 2 of 4 files ever started. The Save-all-as-ZIP path was verified with system tools — `unzip -t` reports no errors, `zipinfo` shows `defN` on all four entries at 99.7% compression, extracted files are byte-identical, and a duplicate filename was uniquified.
  - *Whole-project verification after all five loops*: all 51 Swift files parse. All 22 non-SwiftUI files (every engine, all models, file import, persistence, the batch runner) type-check TOGETHER under `-swift-version 6 -strict-concurrency=complete` at `arm64-apple-macosx14.0`, the exact peer of the app's iOS 17 deployment target: **0 errors, 0 warnings**. The 5 deprecation warnings visible at a macOS 15+ target come from the iOS-18-deprecated `AVAssetExportSession` fallback API and do not fire at iOS 17.
  - *Not verified*: no iOS build, no simulator, no XCTest execution was possible on this machine (Command Line Tools only). All SwiftUI layout and rendering, and the share extension's responder-chain open, are parse-clean and reviewed but unproven.

- [2026-08-01 13:40 CEST]: Release Hardening for 2.0
  - *Details*: Two adversarial review rounds over the compression overhaul found 39 then 24 confirmed defects. This entry covers fixing the release blockers and the honesty problems in the user-facing copy.
  - *Crash and memory*: `TranscodePipeline` cancellation raced `AVAssetReader.startReading()`, which raises an uncatchable Objective-C exception - measured 131 aborts across 760 trials, now 0 across 1400. The preset-fallback path had the identical bug against `AVAssetExportSession` (56 aborts / 160 trials), fixed with the same `markStarted`/`didStart` handshake plus staged stop attempts. The two sample pumps were merged onto a single `sampleQueue` so `tearDown` is genuinely serialised against both, which Apple's headers require and the previous comment incorrectly claimed. `SimpleZIPReader.inflate` allocated the header's declared size unbounded (a 1.2 MB archive peaked at 1204 MB and got the app jetsam-killed); it now enforces per-entry, total and ratio caps and degrades to wrapping. `SimpleZIPWriter` streams through a `FileHandle` instead of building the archive in one `Data`, dropping peak footprint from 3x the input to about 1x.
  - *Compression correctness*: the streaming writer had introduced a "judge the entry by its first 4 MB" rule that stored any entry whose head was incompressible, however well the rest compressed. Removed - the only safe early exit is the one that fires when deflate is genuinely expanding the data - and replaced with a name-based skip so already-compressed formats never pay for a wasted pass. Measured: a 10 MB entry with a 5 MB random head and a 5 MB text tail now deflates at 49.9% where it was previously stored at 0%; a 2 MB JPEG-named entry stores in 0.004 s. `maxEntryCount` was raised from 8192 to the ZIP format's own 16-bit limit, because 8192 rejected ordinary archives.
  - *History persistence*: `HistoryStore.save()` encoded dates as ISO-8601 while `load()` used a bare `JSONDecoder` expecting `.deferredToDate`, so decoding threw on `createdAt` every time and `?? []` swallowed it - Recent had never survived a relaunch, and the next `add()` overwrote the file. `load()` now uses the matching `JSONDecoder.app` (which existed and was called from nowhere) and logs failures instead of discarding them. `history.json` and its directory are excluded from iCloud backup, which is what makes the Settings wording literally true.
  - *App Store submission*: `PrivacyInfo.xcprivacy` declared no `NSPrivacyAccessedAPITypes`, which fails at App Store Connect processing with ITMS-91053 before review can even start. It now declares `NSPrivacyAccessedAPICategoryFileTimestamp` with reason `C617.1` (every timestamp read targets the app container - verified against each call site; `DDA9.1` would have been wrong since the app never displays timestamps) and `NSPrivacyAccessedAPICategoryUserDefaults` with `CA92.1`. Disk-space, boot-time and active-keyboard categories were checked and are genuinely unused. The share extension binary uses no required-reason API, so one manifest in the app bundle is correct. The extension's `NSExtensionActivationRule` capped every attachment type at 1, which hid the extension from the share sheet entirely for multi-selection and silently defeated its multi-attachment support; raised to 25 to match `maximumAttachments`.
  - *Honesty of user-facing copy*: Settings claimed "File names and paths are not logged" while `history.json` stored both and the home screen displayed them. Home claimed "Everything stays on your iPhone" without qualification, which every result screen's share sheet contradicts. Both rewritten, and the two falsified strings removed from `Localizable.xcstrings` so they no longer ship in the bundle. `OutputGuard.warningsAfterKeepingOriginal` now withdraws warnings that describe a rebuild which was discarded, so a user handed back their untouched original is no longer told their form fields stopped working. `zipCouldNotRepack` used to recite "encryption or ZIP64" for every failure including plain Finder-made archives; the message is now derived from the typed `ArchiveRefusal`. `Clear temporary files` now also clears the app group's `Incoming` directory, which held full copies of shared files and was never swept.
  - *Version*: `MARKETING_VERSION` 1.0 -> 2.0 and `CURRENT_PROJECT_VERSION` 1 -> 2 across all 8 build configurations, with the app and share extension matching (App Store Connect rejects a mismatched appex). A major bump rather than a point release: all four compression engines were rewritten and batch compression is new. `Scripts/generate_project.rb` was updated to the same values so a regeneration cannot silently revert them.
  - *Verification*: all 38 app source files type-check together against the real UIKit/SwiftUI/PDFKit via Mac Catalyst (`-target arm64-apple-ios17.0-macabi`, Swift 6, complete strict concurrency, `-warnings-as-errors`) with 0 errors and 0 warnings; the harness was canary-tested against a type error, an iOS-18-only symbol and a genuine Sendable violation. All 54 files parse. Every plist and the privacy manifest pass `plutil -lint`. ZIP output re-validated with `unzip -t` and `zipinfo`.
